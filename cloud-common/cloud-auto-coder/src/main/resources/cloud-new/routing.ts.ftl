import { Routes } from '@angular/router';

import { ${nameClass}Component } from './${nameClass? uncap_first}.component';

export const ${nameClass}Routes: Routes = [{
  path: '',
  component: ${nameClass}Component
}];
